# UNDLM PostFlow - Requirements
# Pipeline de post-production pour documentaire animé

# Core dependencies
requests>=2.28.0
pathlib2>=2.3.0
dataclasses>=0.8; python_version < "3.7"

# Discord integration
discord-webhook>=1.3.0

# Frame.io integration
# Note: Frame.io uses standard requests, no specific package needed

# Google Sheets integration
gspread>=5.7.0
google-auth>=2.16.0
google-auth-oauthlib>=0.8.0
google-auth-httplib2>=0.1.0

# After Effects integration (via ExtendScript)
# Note: Communication via file system, no Python package needed

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: for enhanced logging
colorama>=0.4.6

# Optional: for data analysis
pandas>=1.5.0
numpy>=1.24.0
